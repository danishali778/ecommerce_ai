This folder is intentionally thin.

App-local API helpers belong here only when they add value on top of `@frontend/api-client`,
such as query-key factories or response normalization helpers. Business API contracts stay in
`packages/frontend/api-client`.
